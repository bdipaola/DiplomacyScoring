angular.module("DiplomacyApp")
.controller('BoardShowCtrl', function($scope, $routeParams, $location, boardShow){
	var boardNumber = $routeParams.id;
	
	$scope.board = boardShow;	

	boardShow.loadBoard(boardNumber);

	$scope.viewPlayer = function(playerId){
		$location.url('/players/' + playerId);
	};

	$scope.editBoard = function(boardId){
		$location.url('/boards/' + boardId + '/edit');
	};

})
.$inject = ['$scope','$routeParams', '$location', 'boardShow'];